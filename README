## How to install:

Log in to your MySQL server using a tool like MySQL Workbench or the command line.
Create a new database named 'blogs':
CREATE DATABASE blogs;

## SERVER:

\*) change .env values if needed (mysql password and username)
MYSQL_PASSWORD=""
MYSQL_USERNAME=""

1. run init_db.py
2. run app.py

## CLIENT:

1. npm install
2. npm start
