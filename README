## How to install:

Log in to your MySQL server using a tool like MySQL Workbench or the command line.
Create a new database named 'blogs':
CREATE DATABASE blogs;

## SERVER:

- change .env file values in the backend folder
  MYSQL_PASSWORD="your password"
  MYSQL_USERNAME="your username"

1. run init_db.py
2. run app.py

## CLIENT:

1. cd frontend
2. npm install
3. npm start
